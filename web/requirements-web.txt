# CBZify Web Interface Requirements
# Core conversion dependencies (shared with CLI)
PyMuPDF>=1.23.0
EbookLib>=0.18
Pillow>=10.0.0

# Web framework dependencies
Flask>=3.0.0
Flask-SocketIO>=5.3.0
Werkzeug>=3.0.0

# Optional: Production web server
gunicorn>=21.0.0